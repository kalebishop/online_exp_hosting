build:
  docker:
    web: server/Dockerfile
  config:
    BUILD_ENV: "production"
    OVERCOOKED_BRANCH: "main"
    AGENT_BRANCH: "main"
    GRAPHICS: "overcooked_graphics_v2.2.js"
    FLASK_ENV: "production"
